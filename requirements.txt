# CPU-only PyTorch wheels for Windows/Linux.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
torchvision==0.25.0+cpu
surya-ocr==0.22.1
beautifulsoup4>=4.12.0,<5
python-dotenv>=1.0.0,<2
openai>=2.54.0,<3
mistralai>=2.0.0,<3
pydantic>=2.0,<3
# Protect the shared dashboard/history when multiple test processes save results.
filelock>=3.12,<4
# 0.22 supports Surya's Pillow <11 constraint and provides Windows wheels.
pillow-heif==0.22.0
# Image decoding and PDF rendering for the OpenRouter OCR backend.
Pillow>=10.2.0,<11
pypdfium2>=5.10.1,<6
